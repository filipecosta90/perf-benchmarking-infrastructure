---
    
- name: Configure EC2 instance
  hosts: all
  connection: ssh
  become: yes
  become_user: root
  become_method: sudo
  gather_facts: false

  tasks:
    - name: Install python-apt
      become: yes
      apt:
        pkg:
        - python-apt
        
    
